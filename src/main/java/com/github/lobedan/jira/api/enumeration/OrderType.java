package com.github.lobedan.jira.api.enumeration;

/**
 * jira can have two main order by types like in sql to
 * 1. ASC - lowest to highest
 * 2. DESC - highest to lowest
 *
 * @author svenklemmer
 * @since 0.1.0
 */
public enum OrderType {
  ASC, DESC
}
